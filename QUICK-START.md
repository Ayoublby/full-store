# 🚀 دليل البدء السريع - Full Store

## 📥 خطوة 1: التنصيب

```bash
# استنسخ المشروع
git clone https://github.com/your-username/full-store.git
cd full-store

# ثبّت الحزم
npm install
```

## ▶️ خطوة 2: التشغيل

```bash
# شغّل السيرفر
npm start
```

سيعمل على: `http://localhost:3000`

## 🎯 خطوة 3: الاستخدام

### للعملاء (الواجهة الأمامية)

1. **الصفحة الرئيسية**: `http://localhost:3000`
2. **صفحة المنتجات**: `http://localhost:3000/pages/products.html`
3. **صفحة العربة**: `http://localhost:3000/pages/cart.html`

### للمدير (لوحة التحكم)

1. **افتح لوحة التحكم**: `http://localhost:3000/admin-dashboard-xyz.html`

2. **إضافة منتج جديد**:
   - املأ جميع الحقول
   - ارفع الصور (اسحب أو انقر)
   - اختر الصفحات التي تريد عرض المنتج فيها
   - اضغط "إضافة المنتج"

3. **تعديل منتج**:
   - اذهب إلى تبويب "إدارة المنتجات"
   - اضغط "تعديل" على المنتج المطلوب
   - عدّل البيانات
   - اضغط "تحديث المنتج"

4. **حذف منتج**:
   - في جدول المنتجات، اضغط "حذف"
   - أكد الحذف

## 📂 أين تُحفظ البيانات؟

### المنتجات
```
products.json          ← قاعدة بيانات JSON
products-data.js       ← يتحدث تلقائياً
```

### الصور
```
images/uploaded/       ← الصور المرفوعة
```

## 🔄 رفع إلى GitHub

```bash
# تهيئة Git (إذا لم يكن مهيّأ)
git init
git add .
git commit -m "Initial commit"

# ربط بمستودع GitHub
git remote add origin https://github.com/your-username/full-store.git
git push -u origin main
```

## 🌐 النشر على السيرفر

### باستخدام PM2 (موصى به)

```bash
# تثبيت PM2
npm install -g pm2

# تشغيل المشروع
pm2 start server.js --name full-store

# حفظ التكوين
pm2 save
pm2 startup
```

### باستخدام Node مباشرة

```bash
# في خلفية الشاشة
nohup node server.js > output.log 2>&1 &
```

## ⚙️ تخصيص المنفذ

```bash
# تشغيل على منفذ مختلف
PORT=8080 npm start
```

أو عدّل `server.js`:

```javascript
const PORT = process.env.PORT || 8080;  // غيّر 3000 إلى 8080
```

## 🐛 حل المشاكل الشائعة

### خطأ "Port already in use"
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID [رقم_العملية] /F

# Linux/Mac
lsof -ti:3000 | xargs kill -9
```

### خطأ "Cannot find module"
```bash
rm -rf node_modules package-lock.json
npm install
```

### الصور لا تظهر
```bash
# تأكد من وجود المجلدات
mkdir -p images/uploaded
chmod 755 images/uploaded
```

## 📱 الوصول من أجهزة أخرى

```bash
# احصل على IP address
# Windows
ipconfig

# Linux/Mac
ifconfig

# افتح من جهاز آخر
http://[your-ip]:3000
```

## 🔐 تأمين لوحة التحكم

1. **غيّر اسم ملف الأدمن**:
```bash
mv admin-dashboard-xyz.html admin-secret-panel-abc123.html
```

2. **أضف مصادقة** (اختياري):
   - استخدم middleware للتحقق
   - أو استخدم reverse proxy مع basic auth

## 📊 مراقبة السيرفر

### مع PM2
```bash
# الحالة
pm2 status

# السجلات
pm2 logs full-store

# إعادة التشغيل
pm2 restart full-store

# إيقاف
pm2 stop full-store
```

## 💡 نصائح سريعة

✅ **احتفظ بنسخة احتياطية** من `products.json` بانتظام

✅ **استخدم .gitignore** لتجنب رفع `node_modules/`

✅ **ضغط الصور** قبل رفعها لتحسين الأداء

✅ **اختبر على localhost** قبل النشر للإنتاج

✅ **استخدم HTTPS** في الإنتاج

## 📚 روابط مفيدة

- [التوثيق الكامل](README-GITHUB.md)
- [API Documentation](API.md)
- [أسئلة شائعة](FAQ.md)

---

**جاهز! 🎉 موقعك يعمل الآن**

إذا واجهت أي مشكلة، راجع التوثيق الكامل أو افتح issue على GitHub.
