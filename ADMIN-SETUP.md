# 🛡️ دليل إعداد لوحة التحكم الإدارية - Full Store

## 📋 نظرة عامة

تم إضافة نظام إدارة كامل للمتجر يتضمن:
- ✅ لوحة تحكم إدارية (Admin Panel) بتصميم احترافي
- ✅ إضافة/تعديل/حذف المنتجات
- ✅ رفع وإدارة الصور
- ✅ Backend بسيط باستخدام Node.js + Express
- ✅ تخزين البيانات في ملفات JSON

---

## 🚀 التثبيت والإعداد

### الخطوة 1: تثبيت Node.js

تأكد من تثبيت Node.js (الإصدار 14 أو أحدث):

```bash
# تحقق من النسخة المثبتة
node --version
npm --version
```

إذا لم يكن مثبتاً، قم بتحميله من: https://nodejs.org/

### الخطوة 2: تثبيت المكتبات المطلوبة

في مجلد المشروع، قم بتشغيل:

```bash
npm install
```

سيتم تثبيت:
- **express**: إطار عمل الويب
- **multer**: لرفع الملفات والصور
- **cors**: للسماح بطلبات API

### الخطوة 3: تشغيل السيرفر

```bash
# تشغيل عادي
npm start

# أو للتطوير مع إعادة التشغيل التلقائي (يتطلب nodemon)
npm run dev
```

سيعمل السيرفر على: **http://localhost:3000**

---

## 🔐 الوصول إلى لوحة التحكم

### الرابط المخفي (بدون زر في الموقع)

لوحة التحكم متاحة على الرابط التالي:

```
http://localhost:3000/admin-dashboard-xyz.html
```

**ملاحظة مهمة**: 
- هذا الرابط غير موجود في أي صفحة من صفحات الموقع
- يجب حفظ الرابط والوصول إليه مباشرة
- يمكنك تغيير اسم الملف لمزيد من الأمان

### تغيير اسم صفحة الإدارة (اختياري)

لمزيد من الأمان، يمكنك إعادة تسمية الملف:

```bash
mv admin-dashboard-xyz.html admin-secret-panel-2025.html
```

ثم الوصول عبر:
```
http://localhost:3000/admin-secret-panel-2025.html
```

---

## 📁 بنية الملفات

```
FULL-STORE--main/
│
├── admin-dashboard-xyz.html    # لوحة التحكم الإدارية
├── server.js                   # Backend Server
├── package.json               # ملف المكتبات
├── products.json              # بيانات المنتجات (JSON)
├── products-data.js           # ملف المنتجات الأصلي
│
├── images/
│   ├── uploaded/              # الصور المرفوعة من لوحة التحكم
│   ├── electronics/           # صور الإلكترونيات
│   ├── tools/                # صور الأدوات
│   └── clothes/              # صور الملابس
│
├── css/
│   └── main-styles.css       # ملف التصميم المشترك
│
├── js/
│   ├── cart-system.js
│   ├── product-system.js
│   └── utils.js
│
└── pages/                    # صفحات الموقع
```

---

## ✨ مميزات لوحة التحكم

### 1. إدارة المنتجات
- ✅ إضافة منتجات جديدة
- ✅ تعديل المنتجات الموجودة
- ✅ حذف المنتجات
- ✅ إدارة حالة المخزون
- ✅ تحديد المنتجات المميزة

### 2. إدارة الصور
- ✅ رفع صور متعددة لكل منتج
- ✅ معاينة الصور قبل الحفظ
- ✅ حذف الصور غير المرغوبة
- ✅ السحب والإفلات (Drag & Drop)

### 3. البيانات المدعومة
- اسم المنتج
- الوصف التفصيلي
- السعر
- السعر الأصلي (للخصومات)
- نسبة الخصم (تُحسب تلقائياً)
- الفئة (إلكترونيات، أدوات، ملابس، متنوعة)
- الكمية المحدودة
- حالة المخزون (متوفر/غير متوفر)
- منتج مميز (نعم/لا)
- الصفحات المعروض فيها

### 4. الإحصائيات
- إجمالي المنتجات
- المنتجات المتوفرة
- المنتجات المميزة
- عدد الفئات

---

## 🔧 API Endpoints

السيرفر يوفر API كامل لإدارة المنتجات:

### المنتجات

```javascript
// جلب جميع المنتجات
GET /api/products

// جلب منتج واحد
GET /api/products/:id

// إضافة منتج جديد
POST /api/products
Body: {
  name: "اسم المنتج",
  description: "الوصف",
  price: 100,
  category: "electronics",
  images: ["/images/uploaded/image1.jpg"],
  inStock: true,
  featured: false,
  showInPages: ["index", "products"]
}

// تحديث منتج
PUT /api/products/:id
Body: { /* البيانات المراد تحديثها */ }

// حذف منتج
DELETE /api/products/:id
```

### رفع الصور

```javascript
POST /api/upload-images
Content-Type: multipart/form-data
Field: images (max 10 files)
```

### الإحصائيات

```javascript
GET /api/stats
```

---

## 💾 نظام التخزين

### 1. products.json
ملف JSON يحتوي على جميع بيانات المنتجات بصيغة:

```json
[
  {
    "id": "product-1234567890",
    "name": "اسم المنتج",
    "description": "الوصف",
    "price": 100,
    "category": "electronics",
    "images": ["/images/uploaded/image1.jpg"],
    "inStock": true,
    "featured": false,
    "showInPages": ["index", "products"],
    "addedDate": "2025-01-01T12:00:00.000Z"
  }
]
```

### 2. products-data.js
يتم تحديثه تلقائياً عند كل عملية (إضافة/تعديل/حذف) ليبقى متوافقاً مع الموقع.

### 3. الصور
- تُحفظ في مجلد: `images/uploaded/`
- التسمية: `timestamp-random.jpg`
- يتم حذفها تلقائياً عند حذف المنتج

---

## 🎨 التصميم

لوحة التحكم تستخدم **نفس نظام التصميم** الخاص بالموقع:
- ✅ نفس الألوان (`--primary-color`, `--success-color`, إلخ)
- ✅ نفس الخطوط (Cairo)
- ✅ نفس الأيقونات (Font Awesome)
- ✅ متجاوب مع جميع الشاشات
- ✅ تصميم عربي RTL

---

## 🔒 الأمان

### الحماية الأساسية المطبقة:
1. ✅ رابط مخفي بدون زر في الموقع
2. ✅ التحقق من نوع الملفات (صور فقط)
3. ✅ حد أقصى لحجم الملف (5MB)
4. ✅ التحقق من صحة البيانات

### توصيات إضافية للإنتاج:
- 🔐 إضافة نظام تسجيل دخول (Username + Password)
- 🔐 استخدام JWT للمصادقة
- 🔐 HTTPS فقط
- 🔐 Rate Limiting لمنع الهجمات
- 🔐 قاعدة بيانات حقيقية (MongoDB/PostgreSQL)

---

## 📝 سير العمل

### إضافة منتج جديد:

1. افتح لوحة التحكم
2. انقر على "إضافة منتج جديد"
3. املأ البيانات المطلوبة
4. ارفع الصور (سحب وإفلات أو تحديد)
5. اختر الصفحات المعروض فيها
6. انقر "إضافة المنتج"
7. ✅ يتم حفظ المنتج تلقائياً في products.json و products-data.js

### تعديل منتج:

1. اذهب إلى "إدارة المنتجات"
2. انقر "تعديل" على المنتج المراد
3. سيتم ملء النموذج بالبيانات الحالية
4. عدّل ما تريد
5. انقر "تحديث المنتج"

### حذف منتج:

1. اذهب إلى "إدارة المنتجات"
2. انقر "حذف" على المنتج
3. أكد الحذف
4. ✅ يتم حذف المنتج والصور المرتبطة به

---

## 🌐 النشر على الإنتاج

### خيار 1: Vercel (مجاني)

```bash
# تثبيت Vercel CLI
npm install -g vercel

# تسجيل الدخول
vercel login

# رفع المشروع
vercel
```

### خيار 2: Heroku

```bash
# تثبيت Heroku CLI
# ثم:
heroku login
heroku create full-store-app
git push heroku main
```

### خيار 3: استضافة عادية

1. رفع الملفات عبر FTP
2. تشغيل `npm install` على السيرفر
3. استخدام PM2 لإبقاء السيرفر يعمل:

```bash
npm install -g pm2
pm2 start server.js --name "full-store"
pm2 save
pm2 startup
```

---

## ❓ الأسئلة الشائعة

### س: كيف أغير رابط لوحة التحكم؟
**ج:** أعد تسمية الملف `admin-dashboard-xyz.html` إلى أي اسم تريده.

### س: هل يمكن إضافة نظام تسجيل دخول؟
**ج:** نعم، يمكن إضافة نظام مصادقة باستخدام Passport.js أو JWT.

### س: كيف أنقل البيانات لقاعدة بيانات؟
**ج:** استبدل دوال `readProducts()` و `writeProducts()` في server.js بدوال تتعامل مع قاعدة البيانات.

### س: الصور لا تظهر بعد الرفع؟
**ج:** تأكد من أن مجلد `images/uploaded/` موجود وله صلاحيات الكتابة.

### س: كيف أعمل نسخة احتياطية؟
**ج:** انسخ الملفات التالية:
- `products.json`
- `products-data.js`
- مجلد `images/uploaded/`

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. تحقق من Console في المتصفح (F12)
2. تحقق من لوج السيرفر في Terminal
3. تأكد من تشغيل السيرفر بشكل صحيح
4. تأكد من صلاحيات الملفات

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

## ✅ التحديثات المستقبلية

- [ ] نظام تسجيل الدخول
- [ ] لوحة تحكم للطلبات
- [ ] تقارير المبيعات
- [ ] إدارة المستخدمين
- [ ] نظام الإشعارات
- [ ] تصدير البيانات (Excel/CSV)

---

**تم بنجاح! 🎉**

لوحة التحكم جاهزة للاستخدام على:
**http://localhost:3000/admin-dashboard-xyz.html**
